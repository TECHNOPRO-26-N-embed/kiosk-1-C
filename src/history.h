
#ifndef HISTORY_H
#define HISTORY_H

// history.h: 購入履歴情報の定義

typedef struct {
    // 履歴情報（仮）
    int dummy;
} HistoryStore;

typedef struct {
    // 履歴レコード（仮）
    int dummy;
} HistoryRecord;

#endif // HISTORY_H
